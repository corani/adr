// Package app implements the application logic for each adr command.
// Each function corresponds to a CLI command (Init, Create, List, Show, Edit,
// Update, Version) and orchestrates the adr, config, and template packages.
package app
